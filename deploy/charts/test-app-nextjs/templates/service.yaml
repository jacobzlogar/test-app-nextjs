apiVersion: v1
kind: Service
metadata:
  name: {{ include "test-app-nextjs.fullname" . }}
  labels:
    {{- include "test-app-nextjs.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "test-app-nextjs.selectorLabels" . | nindent 4 }}
